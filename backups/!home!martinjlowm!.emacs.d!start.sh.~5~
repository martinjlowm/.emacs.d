#!/bin/sh

xprop -name emacsclient >/dev/null 2>/dev/null
if [ "$?" = "1" ]; then
    echo "Open new frame"
    echo "$?"
    emacsclient -c -n -a "" "$@"
else
    echo "Use existing frame"
    echo "$?"
    # emacsclient -n -a "" "$@"
fi
